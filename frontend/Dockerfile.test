# Test-specific Dockerfile for running frontend tests
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package.json ./

# Install dependencies (including dev dependencies for testing)
RUN npm install

# Copy source code
COPY . .

# Default command runs tests
CMD ["npm", "test", "--", "--watchAll=false"]
